/vendor/
composer.lock
.DS_Store
.idea/
.vscode/

# Smoke-test fixtures live outside the repo (~/Sites/CommerceWeavers/YouTube/fixtures)
# so client composer.json files never end up here. Keep the directory out of
# version control entirely in case anyone drops one in by accident.
/fixtures/
